var assert = require("assert"), expect = require('expect.js');
    //store = require('../Store/MedicationRouteStore/MedicationRouteStore.js').create();        

describe("Medication Route Store", function () {    
    /*
    it("should be able to get all entries", function (done) {       
       
        store.getAll(function (error, results) {
            expect(error).to.be(null);
            expect(results).not.to.be(undefined);
            expect(results).to.be.an('array');
            expect(results).to.have.length(106);
            done();
        });
    });


    it("should be able to get one entry", function () {

        store.getMedicationByCode("C38193", function (error, results) {
            if (error) {
                console.log("Error getting medication routes");
                console.log(error);
                expect(results.length == 0);
            }
            else {
                console.log(results.length);
                expect(results.length == 114);
            }
        });
    });
    */
});