var assert = require("assert"), expect = require('expect.js');
//var store = require('../Store/MedicationStore/MedicationStore.js').create();

describe("Medication Store", function () {
    /*
    it("should be able to query entries using lowercase", function (done) {
        store.RankedSearch('diperodon', function (error, results) {
            expect(error).to.be(null);
            expect(results).not.to.be(undefined);
            expect(results).to.be.an('array');
            expect(results).to.have.length(9);
            done();
        });
    });
    it("should be able to query entries using mixed case", function (done) {

        store.RankedSearch('DIPerodon', function (error, results) {
            expect(error).to.be(null);
            expect(results).not.to.be(undefined);
            expect(results).to.be.an('array');
            expect(results).to.have.length(9);
            done();
        });
    });

    it("should be able to get one entry", function () {
        store.getMedicationByCUID("798349", function (error, results) {
            expect(error).to.be(null);
            expect(results).not.to.be(undefined);
            //expect(results.Cuid).to.be('798349');
        });
    });

    it("should have the correct number of medications loaded in the database", function () {
        store.getCollection(function (error, collection) {
            expect(error).to.be(null, "An error occurred when retrieving collection");
            collection.count(function (error, count) {
                expect(error).to.be(null, "An error occurred when retrieving count");
                expect(count).to.be(31214); //see the Store/MedicationStore/medications.js file, row count in that file should equal what is in the database
                //console.log(count);
            });
        });
    });
    */
});



